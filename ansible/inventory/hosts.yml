all:
  children:
    radio_station:
      hosts:
        fileserver:
          ansible_host: 192.168.1.x  # change to your fileserver IP
          ansible_user: your_user     # change to your SSH user
